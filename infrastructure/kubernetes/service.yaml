apiVersion: v1

kind: Service

metadata:
  name: terragest-service

spec:

  selector:
    app: terragest-web

  ports:

    - protocol: TCP

      port: 80

      targetPort: 3000

  type: LoadBalancer
