import {
  RuntimeEventRegistry
}
from "@/runtime/events/RuntimeEventRegistry";

export {
  RuntimeEventRegistry
}
from "@/runtime/events/RuntimeEventRegistry";

export const {
  MATERIEL_CREATED,
  MATERIEL_UPDATED,
  MATERIEL_DELETED,
  MATERIEL_BREAKDOWN_DECLARED,
} = RuntimeEventRegistry;